name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
publish_to: none
version: 0.2.8-dev.nullsafety
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.26.0
  esc_pos_utils: 
    git:
      url: https://github.com/in4matic-srl/esc_pos_utils.git
      
  # esc_pos_utils:
    # path: ../esc_pos_utils
  #flutter_bluetooth_basic: ^0.1.5
  flutter_bluetooth_basic: 
    git:
      url: https://github.com/in4matic-srl/flutter_bluetooth_basic.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
